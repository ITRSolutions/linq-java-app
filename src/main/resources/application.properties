spring.application.name=LINQ

# Server port
server.port=8090
#server.servlet.context-path=/linq

# Datasource connection
spring.datasource.platform=mysql
spring.datasource.initialization-mode=always
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.datasource.url=jdbc:mysql://localhost/linqdemodb
spring.datasource.username=itradmin
spring.datasource.password=hulu007k

# Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.hibernate.show-sql=true

# Logging
logging.level.root=INFO
logging.level.org.springframework=ERROR
logging.level.com.numericaideas=DEBUG
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

security.basic.enabled=false


# Enable/Disable DevTools
spring.devtools.restart.enabled=true

# Configure LiveReload (Optional)
spring.devtools.livereload.enabled=true

# Configure restart behavior
#spring.devtools.restart.exclude=**/static/**,**/public/**  # Do not restart for static assets
#spring.devtools.restart.include=**/*.java,**/*.properties,**/*.yml  # Only restart on code/config changes

spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html
# Set this to true in production
spring.thymeleaf.cache=false 
